import {ClientType} from '../../enums/clientType';
import {ProjectStatus} from '../../enums/projectStatus';
import {InputsConfig} from '../../types/inputsconfig';

const settingsInputConfig: InputsConfig[] = [
  {
    title: 'SERVER URL',
    dtoKey:''
  },
];

export {settingsInputConfig};
