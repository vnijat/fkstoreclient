export enum PurchaseStatus {
  IN_PROGRESS = 'in progress',
  CONFIRMED = 'confirmed',
  REJECTED = 'rejected',
}

export enum PaymentMethod {
  CASH = 'cash',
  BANK = 'electronic bank transfer',
}

export enum PurchaseItemStatus {
  IN_PROGRESS = 'in progress',
  CONFIRMED = 'confirmed',
  REJECTED = 'rejected',
}
